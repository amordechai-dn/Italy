# הוספת עמוד חדש

1. יוצרים תיקייה בשם הכתובת הרצויה ובתוכה `index.html` — למשל `hotels/index.html`, שמקבל את הכתובת הנקייה `/hotels/`.
2. מוסיפים ל־`html` את `data-site-root`: בעמוד הבית `data-site-root="."` ובעמוד שבתיקייה `data-site-root=".."`.
3. מוסיפים ל־`body` מזהה עמוד ייחודי: `data-page="page-id"`.
4. טוענים קבצים יחסית לתיקייה (`../site.css`, ‏`../site.js` וכולי), ומוסיפים כתובת `canonical` מלאה ונקייה.
5. מוסיפים קישור דילוג, את `<header class="site-header" data-site-header></header>` ואת מבנה ה־`hero` עם `id="main-content"`.
6. מוסיפים רשומה אחת למערך `SITE_PAGES` שב־`site.js`, כולל `href` נקי ו־`file` של קובץ המקור. הניווט והעמוד הפעיל נוצרים אוטומטית.
7. עיצוב משותף שייך ל־`site.css`; אסור להוסיף `<style>` או `<script>` בתוך HTML ואסור להעתיק רכיבים משותפים לקובץ העמוד.
8. אם הייתה כתובת ישנה, משאירים בה עמוד הפניה קטן לכתובת החדשה כדי שקישורים קיימים לא יישברו.
9. לפני העלאה בודקים את כל העמודים ברוחב מחשב וברוחב טלפון, כולל RTL וללא גלילה אופקית.
10. טבלה רחבה נכנסת ל־`.table-scroll` עם `tabindex="0"`, תיאור גלילה ו־`caption` נגיש.

מריצים `npm run check` לפני העלאה. אותה בדיקה רצה אוטומטית גם ב־GitHub בכל שינוי.

אין להעתיק ידנית את המיתוג או את קישורי הניווט לעמוד חדש.
