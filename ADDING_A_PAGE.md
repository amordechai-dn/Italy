# הוספת עמוד חדש

1. יוצרים קובץ HTML חדש עם `lang="he"`, ‏`dir="rtl"` ו־`<meta name="viewport">`.
2. מוסיפים ל־`body` מזהה עמוד ייחודי: `data-page="page-id"`.
3. טוענים קודם את `site.css`, אחריו קובץ CSS קטן שמכיל רק עיצוב ייחודי לעמוד, ולבסוף את `site.js`.
4. מוסיפים קישור דילוג, את `<header class="site-header" data-site-header></header>` ואת מבנה ה־`hero` עם `id="main-content"`.
5. מוסיפים רשומה אחת למערך `SITE_PAGES` שב־`site.js`. הניווט והעמוד הפעיל נוצרים אוטומטית.
6. עיצוב משותף שייך ל־`site.css`; אסור להוסיף `<style>` או `<script>` בתוך HTML ואסור להעתיק רכיבים משותפים לקובץ העמוד.
7. לפני העלאה בודקים את כל העמודים ברוחב מחשב וברוחב טלפון, כולל RTL וללא גלילה אופקית.
8. טבלה רחבה נכנסת ל־`.table-scroll` עם `tabindex="0"`, תיאור גלילה ו־`caption` נגיש.

מריצים `npm run check` לפני העלאה. אותה בדיקה רצה אוטומטית גם ב־GitHub בכל שינוי.

אין להעתיק ידנית את המיתוג או את קישורי הניווט לעמוד חדש.
